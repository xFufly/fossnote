import type { BunRequest } from "bun";

export async function handleAppelDeconnexion(req: BunRequest): Promise<Response> {
    return new Response(""); // Todo : understand what this function should do and implement it accordingly
}